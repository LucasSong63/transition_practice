import 'package:flutter/material.dart';

class AssetLifecyclePage extends StatelessWidget {
  const AssetLifecyclePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Asset Lifecycle Page');
  }
}
